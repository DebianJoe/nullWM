BUILDING EVILWM

1) Edit Makefile to include the options you need (most people will not need
   to do this).  Instructions are included by any user-editable option.

2) Run 'make'

3) Run 'make install'

That's it.  The README file has information on starting evilwm.

